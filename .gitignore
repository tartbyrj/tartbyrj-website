# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# per-developer Claude Code permissions — never share auto-approve rules
.claude/settings.local.json
.claude/

# generated by `/impeccable init` (2026-08-28) — local design/product working
# docs, not project source of truth. ARCHITECTURE.md is the source of truth
# (see CLAUDE.md); these are kept out of the repo deliberately, not pending.
# NOTE: PRODUCT.md carries the /about → /artist copy edits from 2026-09-02.
# Ignoring it means those live only in this working tree — if it is ever
# tracked, re-check it against the current route names first.
.impeccable/
DESIGN.md
PRODUCT.md
